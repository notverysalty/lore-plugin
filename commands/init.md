---
description: Onboard the current repo to the lore knowledge base (scaffold + pointers + seeding)
---

Use the `init` skill to onboard the current repository: create the `docs/ai-knowledge/` skeleton (contract / gotchas / lore.json), add pointer sections to `CLAUDE.md` and `AGENTS.md`, merge the marketplace config into `.claude/settings.json`, and guide the user through seeding the first batch of real knowledge. Already-onboarded repos only get missing pieces backfilled — nothing is overwritten.
